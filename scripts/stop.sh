#!/bin/bash

# more like a clean-up script before anything else
# ApplicationInstall runs before any other lifecycle event
# and doesn't run the first time
# uses files, script from previously successful run/deployment